<div class="description">
  <%= t("drafts.show.description") %>
</div>

<div class="page-title-actions">
  <%= link_to t("drafts.show.edit"), edit_draft_path(plan) %>,
  <br/>
  <strong>
    <%= link_to(
      t("drafts.show.submit"),
      submissions_path(response_plan_id: plan),
      method: :post,
    ) %>
  </strong>,
  <br/>
  or
  <%= link_to t("drafts.show.destroy"), draft_path(plan), method: :delete %>.
</div>
