<div class="nested-fields section-body">
  <% if f.object.persisted? %>
    <div class="input">
      <%= image_tag f.object.source %>
    </div>
  <% else %>
    <%= f.input :source, label: "Image" %>
  <% end %>

  <%= link_to_remove_association "- Remove image", f %>
</div>
